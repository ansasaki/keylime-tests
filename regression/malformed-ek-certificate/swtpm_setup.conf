# swtpm_setup configuration file for malformed certificate test (swtpm_cert version)
# This configuration tells swtpm_setup to use our swtpm_cert-based certificate creation script

# Use our custom swtpm_cert script to create certificates
create_certs_tool = TESTDIR/create_malformed_certs.sh

# Standard swtpm_setup options
create_certs_tool_config = /dev/null
create_certs_tool_options = 

# Other settings
tss_tcti_tabrmd_bus_type = system
